# Environment for the test suites only (loaded via --env-file in package.json).
# Points at the throwaway Postgres that `sh test/pgtest.sh start` manages —
# never at a real database. Contains no secrets by design.
DATABASE_URL=postgresql://127.0.0.1:54329/taskforge_test
JWT_SECRET=taskforge-test-only-secret
NODE_ENV=test
